
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: multiclustersets.multicluster.crd.antrea.io
spec:
  group: multicluster.crd.antrea.io
  names:
    kind: MultiClusterSet
    listKind: MultiClusterSetList
    plural: multiclustersets
    singular: multiclusterset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MultiClusterSet is the Schema for the multiclustersets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MultiClusterSetSpec defines the desired state of MultiClusterSet
            properties:
              leaders:
                description: Leaders include leader clusters known to the member clusters.
                items:
                  description: MemberCluster defines member cluster information.
                  properties:
                    clusterID:
                      description: Identify member cluster in multi-cluster set.
                      type: string
                    secret:
                      description: Secret name to access API server of the member
                        from the leader cluster.
                      type: string
                    server:
                      description: API server of the member or the leader cluster.
                      type: string
                    serviceAccount:
                      description: ServiceAccount used by the member cluster to access
                        into leader cluster.
                      type: string
                  type: object
                type: array
              members:
                description: Members include member clusters known to the leader clusters.
                  Used in leader cluster.
                items:
                  description: MemberCluster defines member cluster information.
                  properties:
                    clusterID:
                      description: Identify member cluster in multi-cluster set.
                      type: string
                    secret:
                      description: Secret name to access API server of the member
                        from the leader cluster.
                      type: string
                    server:
                      description: API server of the member or the leader cluster.
                      type: string
                    serviceAccount:
                      description: ServiceAccount used by the member cluster to access
                        into leader cluster.
                      type: string
                  type: object
                type: array
              namespace:
                description: Namespace to connect to in leader clusters,. Used in
                  member cluster.
                type: string
            type: object
          status:
            description: MultiClusterSetStatus defines the observed state of MultiClusterSet
            properties:
              leaderStatus:
                items:
                  description: LeaderClusterStatus defines leader cluster status in
                    the member clusters.
                  properties:
                    clusterID:
                      description: ClusterID identifies leader cluster in the set.
                      type: string
                    error:
                      description: Error reports of the leader cluster.
                      type: string
                    status:
                      description: Status indicates connection status between leader
                        and member cluster.
                      type: string
                  type: object
                type: array
              memberStatus:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                items:
                  description: MemberClusterStatus defines member cluster status in
                    the leader clusters.
                  properties:
                    clusterID:
                      description: ClusterID identifies member cluster in the set.
                      type: string
                    error:
                      description: Error reports of the member cluster.
                      type: string
                    isLeader:
                      description: IsLeader is true if the member cluster has selected
                        this cluster as leader.
                      type: boolean
                    status:
                      description: Status indicates connection status between leader
                        and member cluster.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
