
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: federations.mcs.crd.antrea.io
spec:
  group: mcs.crd.antrea.io
  names:
    kind: Federation
    listKind: FederationList
    plural: federations
    singular: federation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Federation is the Schema for the federations API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FederationSpec defines the desired state of Federation.
            properties:
              members:
                description: Member clusters.
                items:
                  properties:
                    clusterID:
                      description: Identify member cluster in federation.
                      type: string
                    secret:
                      description: Secret name to access API server of the member
                        cluster.
                      type: string
                    server:
                      description: API server of the member cluster.
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: FederationStatus defines the observed state of Federation.
            properties:
              memberStatus:
                description: Member cluster status.
                items:
                  properties:
                    clusterID:
                      description: Identify member cluster in federation.
                      type: string
                    error:
                      description: Error is current error, if any.
                      type: string
                    isLeader:
                      description: Identify if leader.
                      type: boolean
                    status:
                      description: Status indicates connection status
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
